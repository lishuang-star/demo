git init命令初始化一个git仓库

在执行完成 git init 命令后，Git 仓库会生成一个 .git 目录，
该目录包含了资源的所有元数据，其他的项目目录保持不变
（不像 SVN 会在每个子目录生成 .svn 目录，Git 只在仓库的根目录生成 .git 目录）

git clone从现有git仓库中拷贝项目
git clone <repo> <directory>
参数说明：
repo:Git 仓库。
directory:本地目录。





git技能问题相关：
介绍你的最常用工作流程
分支 -> 合并 的流程和基本操作方法
fork -> 合并 的流程和基本操作方法

git branch 没有参数时，git branch 会列出你在本地的分支。


如何解决版本冲突

如何可视化的查看存在冲突状态的文本文件具体冲突位置、差异？

如何发送 Pull Request (PR) / Merge Request (MR)

如何合并或拒绝 PR / MR

我们内网 GitLab 上已经启用 git-lfs 的仓库，如何实现：
如何将一个具体文件、扩展名相同的一类文件、一个指定目录下的文件设置为由 git-lfs 管理（俗称大文件）？

下载或更新仓库内容时，对于已经被设置为 大文件 类型的文件如何避免被下载完整文件到本地？

如何下载指定 大文件 的完整文件到本地？

已经推送到远程仓库的 大文件，如何在删除本地完整文件的同时不影响远程仓库中已经上传成功的 大文件？